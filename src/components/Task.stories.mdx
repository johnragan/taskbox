import { Meta, Story } from '@storybook/addon-docs/blocks';
import { taskData, actionsData } from "./Task2.stories2";

import Task from "./Task";

<Meta title="Task" component={Task} />

# Task

You can add whatever Markdown you want to this documentation.

## Displays a Default Task

<Story name="Default"
    args={{
      id: "1",
      title: "Test Task",
      state:"TASK_INBOX",
      updatedAt: new Date(2018, 0, 1, 9, 0),
    }}
    >
  {(args) => <Task task={{ ...taskData }} {...actionsData}  />}
</Story>

## Displays a Pinned Task

<Story name="Pinned"
    args={{
      id: "1",
      title: "Test Task",
      state:"TASK_INBOX",
      updatedAt: new Date(2018, 0, 1, 9, 0),
    }}
    >
  {(args) => <Task task={{ ...taskData, state: "TASK_PINNED" }} {...actionsData} />}
</Story>

## Displays a Archived Task

<Story name="Archived"
    args={{
      id: "1",
      title: "Test Task",
      state:"TASK_INBOX",
      updatedAt: new Date(2018, 0, 1, 9, 0),
    }}
    >
  {(args) => <Task task={{ ...taskData, state: "TASK_ARCHIVED" }} {...actionsData} />}
</Story>


export const Pinned = () => (
  <Task task={{ ...taskData, state: "TASK_PINNED" }} {...actionsData} />
);

export const Archived = () => (
  <Task task={{ ...taskData, state: "TASK_ARCHIVED" }} {...actionsData} />
);

